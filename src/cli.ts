#!/usr/bin/env node
/**
 * CLI entry point for ALPS LSP server
 *
 * Usage:
 *   alps-lsp --stdio    # Start server using stdio
 *   alps-lsp --socket   # Start server using socket (default port 7000)
 */

import './server';
